/**
 * Global feature flag for End-to-End Encryption (E2EE).
 * Set to `true` to re-enable encryption for outgoing messages.
 * Set to `false` to send messages as plain text without deleting E2EE codebase.
 */
export const ENABLE_E2EE = false;
